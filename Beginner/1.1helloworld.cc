#include <stdio.h>      //To use printf 

int main()

{    
    
    // Body of the Function
    // Every statement ends with ; in C

    printf ("Hello world!! \n");
    printf ("\tWelcome to CS220\n") ;
    printf("253");  

   
    // printf always expects a string so "printf(253)" will create an error.
    
    // We cant pass more than one string also. so "printf("23","35") will create an error

    // C doesnot include a new line by default unlike Python
}
